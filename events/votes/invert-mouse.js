const TwitchEvents = require(`../../modules/twitchevents`);


module.exports = {
    data: {
        name: "invert-mouse",
        desc: "Invert mouse",
        type: TwitchEvents.Types.VOTE
    },

    func: {},


    async enable(client) {
        
    },

    async disable(client) {
        
    }
}
